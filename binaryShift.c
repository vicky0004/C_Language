#include <stdio.h>
int main()
{
	int a;
	printf("Enter the value: ");
	scanf("%d",&a);
	b=a>>2;
	a=a<<2;
	printf("the left shifted data is = %d",a);
	printf("\n the right shifted data is = ,b);
	return 0;
}
